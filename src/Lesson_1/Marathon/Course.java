package Lesson_1.Marathon;

public class Course {
    public Course(int length, int height, int height,int length){
       this.length = length;
    }
    int Cross = 80;
    int wall =
    Obstacle[] course = {new Cross(80), new Wall(2), new Wall(1), new Cross(120)};
}
